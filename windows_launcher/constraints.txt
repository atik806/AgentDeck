# Exact versions for a reproducible release build -- the full dependency
# closure of requirements.txt + requirements-build.txt + ./voice_capture, as
# resolved on Windows / CPython 3.11 (the only supported build target).
#
# The release workflow installs with `-c constraints.txt`, so a new upstream
# release inside a requirements.txt range can't silently change what ships.
# Regenerate after an intentional bump:
#
#     .venv-build\Scripts\python -m pip freeze > windows_launcher/constraints.txt
#     # then strip the editable/self lines (-e, voice_capture, AgentDeck)
#
# NOTE: this pins versions, not wheel hashes. A hash-locked install
# (`pip install --require-hashes`) is the next step and needs per-wheel digests
# generated with pip-tools against this same platform.

altgraph==0.17.5
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
colorama==0.4.6
idna==3.19
numpy==1.26.4
packaging==26.3
pefile==2024.8.26
platformdirs==4.11.5
pycparser==3.0
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
PySide6==6.11.2
PySide6_Addons==6.11.2
PySide6_Essentials==6.11.2
pyte==0.8.2
pywhispercpp==1.5.1
pywin32-ctypes==0.2.3
pywinpty==3.0.5
requests==2.34.2
ruamel.yaml==0.18.17
ruamel.yaml.clib==0.2.15
shiboken6==6.11.2
sounddevice==0.5.6
tomlkit==0.15.1
tqdm==4.70.0
urllib3==2.7.0
velopack==0.0.1589.dev41669
wcwidth==0.8.3
webrtcvad-wheels==2.0.14
